Desktop Software Development
Developer needed for creating a Java program to create an HTML report from an Excel file.

1) The program will be started with an Excel file as input.

2) The program will save data, together with time of run. This will be done in a MySQL database.

3) In addition to the Excel data, the database will also contain configuration tables, and auxiliary information (property-update notes).

4) It will generate the report (as per email sample provided).

5) It will email the report to designated recipients. Use javax.mail library. Details of SMTP server using SSL will be provided. Content in text and html format.

6) Program will be a desktop application and should be written in Java. Must follow code style.

For specifications, please see the sample sheet attached.